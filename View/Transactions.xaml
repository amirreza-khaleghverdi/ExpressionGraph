<UserControl x:Class="Page_Navigation_App.View.Transactions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             xmlns:view="clr-namespace:Page_Navigation_App.View"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:TransactionVM />
    </UserControl.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,17,0,10">
            <TextBlock Text="Enter Expression:" Foreground="#00C2FF" Margin="0,0,0,5"/>
            <TextBox Background="#272B2F" Foreground="White" Text="{Binding InputExpression, UpdateSourceTrigger=PropertyChanged}"
                 Height="35" VerticalContentAlignment="Center" Padding="5"/>
            <Button Background="#272B2F" Foreground="#00C2FF" Content="Calculate &amp; Draw" 
                Command="{Binding CalculateCommand}"
                Margin="0,10,0,0" Height="35"/>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="2">

            <ListBox x:Name="TaskListBox" 
             BorderThickness="0" 
             Background="Transparent" 
             ItemsSource="{Binding Tasks}"
             SelectedItem="{Binding SelectedTask}"
             HorizontalContentAlignment="Center" 
             ScrollViewer.VerticalScrollBarVisibility="Hidden">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="15" Margin="5,2" Padding="5">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Name}" FontSize="10" FontWeight="SemiBold" Foreground="#2D3436"/>
                                    <Border Background="{Binding PriorityColor}" CornerRadius="5" HorizontalAlignment="Left" 
                                    Padding="10,2" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding CurrentPriority}" FontSize="6" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <StackPanel.Resources>
                                        <Style TargetType="Button">
                                            <Setter Property="Width" Value="25"/>
                                            <Setter Property="Height" Value="25"/>
                                            <Setter Property="Margin" Value="5,0"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Grid>
                                                            <Ellipse x:Name="circle" Fill="{TemplateBinding Background}"/>
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="circle" Property="Opacity" Value="0.8"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </StackPanel.Resources>

                                    <Button Content="H" Background="#FF3B30"
                                    Command="{Binding SetPriorityCommand}"
                                    CommandParameter="HIGH"/>

                                                                <Button Content="M" Background="#FF9500"
                                    Command="{Binding SetPriorityCommand}"
                                    CommandParameter="MEDIUM"/>

                                                                <Button Content="L" Background="#8E8E93"
                                    Command="{Binding SetPriorityCommand}"
                                    CommandParameter="LOW"/>


                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <TextBlock Grid.Row="2" 
               Text="{Binding ResultValue}" 
               Foreground="#00C2FF" 
               FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,10"/>

        <Border Grid.Row="2" Background="#272B2F" CornerRadius="15" Margin="0,40,0,0">
            <view:ExpressionGraphView TreeRoot="{Binding CurrentTree}"/>
        </Border>
    </Grid>
    
</UserControl>
